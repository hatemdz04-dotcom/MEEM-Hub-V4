(()=>{const state=MEEM_STORE.load();let data=structuredClone(MEEM_DATA);const t=k=>(MEEM_LANG[state.language]||MEEM_LANG.fr)[k]||k;const esc=s=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));function theme(){document.body.classList.toggle('dark',state.theme==='dark');document.documentElement.lang=state.language;document.documentElement.dir=state.language==='ar'?'rtl':'ltr'}function nav(){return `<aside class="sidebar"><div class="logo"><div class="logo-mark">M</div><div><h1>MEEM Hub</h1><p>Mechanical Engineering</p></div></div><nav class="nav"><button data-page="home">🏠 ${t('home')}</button><button data-page="modules">📚 ${t('modules')}</button><button data-page="library">📁 ${t('library')}</button><button data-page="planning">📅 ${t('planning')}</button><button data-page="stats">📊 ${t('stats')}</button><button data-page="badges">🏆 ${t('badges')}</button><button data-page="settings">⚙️ ${t('settings')}</button></nav><div class="sidebar-note">« La discipline aujourd'hui construit la liberté de demain. »</div></aside>`}function side(){return `<aside class="rightbar"><div class="profile"><div class="avatar">👨‍🎓</div><div><h3>${esc(state.user?.name||data.user.name)}</h3><p>${t('semester')} ${data.user.semester}</p></div></div><div class="xp"><b>Niveau ${data.user.level}</b> · ${data.user.xp} XP<div class="xpbar"><span></span></div></div><div class="side-section"><h3>🔥 ${data.user.streak} jours</h3></div><div class="side-section"><h3>${t('tasks')}</h3>${data.tasks.map((x,i)=>`<label class="task"><input type="checkbox" data-task="${i}" ${x.done?'checked':''}>${esc(x.text)}</label>`).join('')}</div></aside>`}function card(m){return `<div class="module"><div class="module-head"><h3>${esc(m.name)}</h3><span class="tag">S${m.semester}</span></div><p>${esc(m.category)}<br>👨‍🏫 ${esc(m.teacher)}</p><div class="progress"><span style="width:${m.progress}%"></span></div><small>${m.progress}% · Coef. ${m.coefficient}</small></div>`}function home(){return `<div class="page active" id="page-home"><section class="hero"><div><p>${t('greeting')}, <b>${esc(state.user?.name||data.user.name)}</b> 👋</p><h2>${t('engineer')}</h2><h3>${t('effort')}</h3></div><div class="hero-quote">${t('quote')}</div></section><div class="grid4"><div class="card stat">🎓<div class="label">${t('average')}</div><strong>${data.user.generalAverage} / 20</strong><small>Objectif ≥ 16</small></div><div class="card stat">📖<div class="label">${t('validated')}</div><strong>${data.user.validatedModules} / ${data.user.totalModules}</strong><small>Cette année</small></div><div class="card stat">✅<div class="label">${t('td')}</div><strong>${data.user.tdTpAverage} / 20</strong><small>Moyenne actuelle</small></div><div class="card stat">🎯<div class="label">${t('objective')}</div><strong>${t('semester')} ${data.user.semester}</strong><small>En cours</small></div></div><section class="section"><div class="section-head"><h2>📚 ${t('modules')}</h2></div><div class="modules">${data.modules.map(card).join('')}</div></section><section class="section"><div class="section-head"><h2>📁 ${t('quickLibrary')}</h2></div><div class="library">${[['📕','Cours'],['📝','TD'],['🧪','TP'],['📋','Examens'],['🎬','Vidéos']].map(x=>`<div class="lib-item"><span>${x[0]}</span><b>${x[1]}</b></div>`).join('')}</div></section></div>`}function modules(){return `<div class="page" id="page-modules"><div class="panel"><h2>📚 ${t('modules')}</h2>${data.modules.map(m=>`<div class="module-row"><div><b>${esc(m.name)}</b><div class="muted">${esc(m.teacher)} · S${m.semester} · Coef. ${m.coefficient}</div></div><span class="tag">${m.progress}%</span></div>`).join('')}</div></div>`}function settings(){return `<div class="page" id="page-settings"><div class="panel"><h2>⚙️ ${t('settings')}</h2><h3>${t('personal')}</h3><div class="form-grid"><div class="field"><label>${t('name')}</label><input id="user-name" value="${esc(state.user?.name||data.user.name)}"></div><div class="field"><label>${t('specialty')}</label><input id="user-specialty" value="${esc(state.user?.specialty||data.user.specialty)}"></div><div class="field"><label>${t('language')}</label><select id="language"><option value="fr">Français</option><option value="ar">العربية</option><option value="en">English</option></select></div><div class="field"><label>${t('theme')}</label><select id="theme"><option value="light">${t('light')}</option><option value="dark">${t('dark')}</option></select></div></div><div class="actions"><button class="btn" id="save">${t('save')}</button><button class="btn secondary" id="reset">${t('reset')}</button></div><div class="side-section"><h3>${t('academic')}</h3><p class="muted">Les données académiques globales seront administrées séparément. Chaque utilisateur gardera ses préférences personnelles.</p><div class="actions"><button class="btn secondary" data-page="modules">${t('editModules')}</button><button class="btn secondary">${t('teachers')}</button></div></div></div></div>`}function simple(id,title){return `<div class="page" id="page-${id}"><div class="panel"><h2>${title}</h2><p class="muted">Cette section sera connectée au backend dans l'étape suivante.</p></div></div>`}function render(){theme();document.getElementById('app').innerHTML=`<div class="app-shell">${nav()}<main class="main"><div class="topbar"><b>MEEM Hub v${MEEM_CONFIG.version}</b></div>${home()}${modules()}${simple('library','📁 '+t('library'))}${simple('planning','📅 '+t('planning'))}${simple('stats','📊 '+t('stats'))}${simple('badges','🏆 '+t('badges'))}${settings()}</main>${side()}</div><div class="toast" id="toast"></div>`;bind();show('home')}function bind(){document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>show(b.dataset.page));document.querySelectorAll('[data-task]').forEach(x=>x.onchange=()=>data.tasks[x.dataset.task].done=x.checked);const l=document.getElementById('language'),th=document.getElementById('theme');if(l)l.value=state.language;if(th)th.value=state.theme;document.getElementById('save')?.addEventListener('click',()=>{state.user=state.user||{};state.user.name=document.getElementById('user-name').value.trim()||data.user.name;state.user.specialty=document.getElementById('user-specialty').value.trim()||data.user.specialty;state.language=l.value;state.theme=th.value;MEEM_STORE.save(state);render();toast(t('saved'))});document.getElementById('reset')?.addEventListener('click',()=>{Object.assign(state,MEEM_STORE.reset());render()})}function show(id){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById('page-'+id)?.classList.add('active');document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.page===id))}function toast(m){const x=document.getElementById('toast');if(!x)return;x.textContent=m;x.style.display='block';setTimeout(()=>x.style.display='none',2000)}render()})();
